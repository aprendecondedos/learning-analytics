{
  "name": "restify",
  "version": "0.0.1",
  "main": "index.js",
  "dependencies": {
    "bunyan": "^0.22.1",
    "restify": "~4.0.3"
  },
  "devDependencies": {
    "grunt": "~0.4.1",
    "grunt-jasmine-nodejs": "^1.3.0",
    "grunt-jscs": "^2.8.0",
    "grunt-jslint": "^1.1.12"
  },
  "scripts": {
    "test": "grunt",
    "start": "SET \"NODE_ENV=development\" && grunt && node --debug --harmony index.js",
    "production": "SET \"NODE_ENV=production\" && node --harmony index.js",
    "local": "SET NODE_ENV=local nodemon --debug --harmony index.js"
  }
}
